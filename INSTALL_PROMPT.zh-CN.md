# 一键安装提示词

```text
从 https://github.com/wannds/seedance-agent-skill 安装 Codex skill。克隆仓库，将内容复制到 $CODEX_HOME/skills/drama-video-generation，将 .env.example 复制为 .env；如果没有配置，向我询问 DRAMA_BASE_URL 和 DRAMA_API_KEY。网关地址和 API 密钥由用户自行填写，不要填入任何厂商默认值。API 密钥只能保存在 .env，不能提交。运行 python scripts/drama_video.py --lang zh --help 验证安装；用户填写配置后运行 python scripts/drama_video.py --lang zh models，确认安装路径和可用模型。
```
