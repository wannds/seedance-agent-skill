interface:
  display_name: Video Generation
  short_description: Generate videos through a configured New API gateway
  default_prompt: Generate a video with the configured gateway using the user's prompt, duration, resolution, aspect ratio, and references.
