interface:
  display_name: Video Generation
  short_description: Generate Seedance videos through a New API gateway
  default_prompt: Generate a Seedance video with the configured gateway using the user's prompt, duration, resolution, aspect ratio, and references. Use only model ids returned by the configured gateway.
