interface:
  display_name: Video Generation
  short_description: Generate gateway videos with A-series Seedance models
  default_prompt: Generate a video with the configured gateway using the user's prompt, duration, resolution, aspect ratio, and references. Use the model ids returned by the configured gateway.
